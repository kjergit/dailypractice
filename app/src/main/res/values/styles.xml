<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/app_blue</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/app_blue</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@color/app_white</item>

    </style>
    <!--设置状态栏颜色-->
    <style name="StatusBarColorTranslucentTheme" parent="AppTheme"></style>
    <!-- 状态栏透明 自适应-->
    <style name="StatusBarTranslucentTheme" parent="AppTheme"></style>

    <!--启动页-->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>
